{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "schema.json#",
  "title": "Datetime Range Extension",
  "description": "STAC Datetime Range Extension to a STAC Item.",
  "allOf": [
    {
      "$ref": "../../../master/item-spec/json-schema/item.json"
    },
    {
      "$ref": "#/definitions/datetime-range"
    }
  ],
  "definitions": {
    "datetime-range": {
      "type": "object",
      "required": [
        "properties"
      ],
      "properties": {
        "properties": {
          "type": "object",
          "required": [
            "start_datetime",
            "end_datetime"
          ],
          "properties": {
            "start_datetime": {
              "title": "Start Date and Time",
              "description": "The searchable start date/time of the assets, in UTC (Formatted in RFC 3339) ",
              "type": "string",
              "format": "date-time",
            }, 
            "end_datetime": {
              "title": "End Date and Time", 
              "description": "The searchable end date/time of the assets, in UTC (Formatted in RFC 3339) ",                  
              "type": "string",
              "format": "date-time",
            }
          }
        }
      }
    }
  }
}