'use strict';

module.exports = {
    help: require('./help'),
    validate: require('./validate'),
    compile: require('./compile'),
    migrate: require('./migrate'),
    test: require('./test')
};
